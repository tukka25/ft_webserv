{
	-H: Used to set headers.
	-X: Used to set the request method.
	-d: Used to send data in the request body (for POST requests).
}

curl localhost:4331 -X DELETE
curl localhost:4332 -X POSTT
curl localhost:4331 -X POST
curl localhost:4331 -X GET